Madestone Sports - Backend

This is a minimal NestJS-style backend prototype using TypeORM and SQLite.

Setup

From the backend folder:

1. Install dependencies
   npm install

2. Start in dev mode
   npm run start:dev

The server listens on http://localhost:3001

Seed: On first run create users via the API or extend the code to auto-seed a CEO user.
